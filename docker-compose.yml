services:
  core-banca-web:
    image: andrescarrillo2001/banquito-core-bancaweb-fronend:latest
    container_name: banquito-core-bancaweb-frontend
    restart: unless-stopped
    ports:
      - "80:3000"
    env_file:
      - .env
